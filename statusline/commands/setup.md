---
description: "Install and configure the Claude Code statusline"
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/setup.sh:*)"]
---

Run this and display the exact output:

```!
"${CLAUDE_PLUGIN_ROOT}/scripts/setup.sh"
```

Do not summarize, interpret, or add anything. Show the raw output only.

If the script fails, help the user troubleshoot based on the error output.
